<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.veterinaria">
    <!-- PAQUETE BASE -->
    <!-- PERMISO ACCESO A INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- PERMISO ACCESO A ESTADO DE LA CONEXION A INTERNET -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- PERMISOS DE CÁMARA Y ALMACENAMIENTO (Como el profe) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Veterinaria"
        tools:targetApi="31">
        <activity
            android:name=".AlertasVet"
            android:exported="false" />
        <activity
            android:name=".RegistrosVet"
            android:exported="false"
            android:theme="@style/Theme.Veterinaria" />
        <activity
            android:name=".CitasVet"
            android:exported="false" />
        <activity
            android:name=".InicioVet"
            android:exported="false" />
        <activity
            android:name=".VeterinarioHomeActivity"
            android:exported="false" />
        <activity
            android:name=".InsertarAnimalActivity"
            android:exported="false" />
        <activity
            android:name=".AdminHomeActivity"
            android:exported="false" /> <!-- MainActivity (Login) es la pantalla de inicio -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>